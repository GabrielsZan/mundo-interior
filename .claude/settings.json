{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"c:\\Users\\gabri\\Desktop\\AppRpg Claude\" && git add -A && git diff --cached --quiet || (git commit -m \"Auto-save: $(date '+%Y-%m-%d %H:%M:%S')\" && git push origin main)"
          }
        ]
      }
    ]
  }
}
